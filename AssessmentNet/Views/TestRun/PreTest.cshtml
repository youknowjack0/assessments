@using System.Web.UI.WebControls
@model AssessmentNet.Models.TestRun

@{
    ViewBag.Title = "Test " + @Model.Test.Name;
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>@ViewBag.Title</h2>

<h4>Instructions:</h4>

<ul>
    <li>Once you click the "Begin test" link below, you will have <b>@string.Format("{0:0.00}", @Model.Test.MaxDurationInHours)</b> hours to complete the test</li>
    <li>Each question has an individual time limit. The time limit may not be the same for every test</li>
    <li>Please attempt to answer every question, points are not subtracted for incorrect answers</li>
    <li>Only completely-correct answers can score points. Partially correct answers do not score any points</li>
    <li>Please submit answers before the time limit expires. There is a small grace-period if you submit answers just after the time limit expires.</li>
    <li>If the time limit for a question expires, the test will <b>not</b> advance to the next question automatically. You can take a break and continue when you're ready.</li>
    <li>If you encounter an error, please record the error, then attempt to refresh or go back. </li>    
    <li><b>It is important to take this test without external assistance.</b> Test results WILL be verified at in-person interviews.</li>
</ul>

<p>@Html.ActionLink("Begin Test", "BeginTestRun", new{testrun = Model.Id})</p>
